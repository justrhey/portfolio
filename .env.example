# Contact form — SERVER-SIDE ONLY, never exposed to the client bundle.
# Copy to .env.local and fill in, and set the same values in the Vercel dashboard.
CONTACT_EMAIL=
RESEND_API_KEY=
# Optional: enables per-IP rate limiting (3 submissions/min). Leave blank to disable.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
